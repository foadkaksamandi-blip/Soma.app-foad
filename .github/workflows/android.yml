name: Android CI (soma.app.foad)

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Set up Gradle (no wrapper, no cache)
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: 8.7
          cache-disabled: true

      - name: Build Buyer/Seller APKs (verbose)
        run: |
          set -o pipefail
          gradle --version
          # بیلد با لاگ کامل
          gradle assembleBuyerDebug assembleSellerDebug --info --stacktrace --no-daemon | tee gradle-build.log

      # این مرحله، خلاصه‌ی خطاها را مستقیماً در کنسول نشان می‌دهد
      - name: Show concise Kotlin/AAPT errors
        if: failure()
        run: |
          echo "================ ERROR SUMMARY ================"
          grep -E "^(e: |.*Android resource linking failed|> Task :app:|> Task :)" -n gradle-build.log | tail -n 200 || true
          echo "==============================================="

      - name: Upload APK artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: soma-apks
          path: |
            app/build/outputs/apk/buyer/debug/*.apk
            app/build/outputs/apk/seller/debug/*.apk

      - name: Upload Gradle logs (always)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: gradle-logs
          path: gradle-build.log
